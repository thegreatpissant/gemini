#ifndef VALIDATOR_H
#define VALIDATOR_H

#include <vector>
#include <string>
#include "gemini_types.h"
std::vector<std::size_t> validate_source (std::vector<std::string> source_file);

#endif // VALIDATOR_H
